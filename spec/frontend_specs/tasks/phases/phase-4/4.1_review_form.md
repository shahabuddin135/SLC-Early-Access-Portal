```slc
@block TASK fe_4.1_review_form
priority: critical
intent: "Build the review submission form component"
scope: phase-4
depends_on: [fe_3.2_download_flow]
estimate: 30
status: todo

content:
  - Add to lib/api.ts:
      - async function submitReview(token, data): Promise<{ok: bool, error?: string}>
          - POST BACKEND_URL/api/v1/review with Authorization header and body
          - Return success or error

  - Create app/actions/review.ts (Server Action):
      - "use server"
      - async function reviewAction(formData: FormData):
          - Get token from cookie
          - Call submitReview(token, {project_link, review_text})
          - Return result

  - Create components/ReviewForm.tsx:
      - Client component ("use client")
      - Fields:
          - project_link: input type="url", placeholder="https://github.com/you/slc-project"
          - review_text: textarea, rows=5, placeholder="Share your experience with the SLC framework..."
      - Submit button: "Submit Review"
      - States:
          - idle: show form
          - loading: show spinner, disable button
          - success: show "✓ Review submitted! Thank you." (green), hide form
          - error: show error message in red
      - Section heading: "Share Your Experience"
      - Subheading: "Submit the project you built using the SLC framework"

  - Design requirements:
      - Section has orange top border accent
      - Textarea styled same as .input class
      - Success state uses var(--color-success) green
      - Smooth fade-in animation when form appears

acceptance_criteria:
  - ReviewForm only visible when has_downloaded == true
  - Valid submission calls POST /review and shows success state
  - 403 error shows "Please download the SLC files first"
  - 422 error shows field validation messages
  - Form disables during submission
  - Success state persists (form hidden after submit)
@end
```
