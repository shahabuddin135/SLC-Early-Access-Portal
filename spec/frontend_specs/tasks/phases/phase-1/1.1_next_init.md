```slc
@block TASK fe_1.1_next_init
priority: critical
intent: "Initialize Next.js 14+ project with TypeScript and App Router"
scope: phase-1
depends_on: none
estimate: 15
status: todo

content:
  - Run: npx create-next-app@latest frontend --typescript --app --no-tailwind --eslint --src-dir=no --import-alias="@/*"
  - Install additional dependencies:
      npm install @vercel/geist
  - Verify: npm run dev starts without errors on localhost:3000
  - Set up .env.local:
      BACKEND_URL=http://localhost:8000
  - Add .env.local to .gitignore
  - Create .env.local.example with BACKEND_URL=

acceptance_criteria:
  - Next.js 14+ App Router project initialized
  - TypeScript configured
  - No Tailwind CSS (Vanilla CSS only per CONSTRAINTS.md)
  - npm run dev starts on localhost:3000
  - .env.local exists with BACKEND_URL
@end
```
