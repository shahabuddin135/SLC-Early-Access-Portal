```slc
@block TASK fe_1.2_design_system
priority: critical
intent: "Implement the design system: CSS tokens, global styles, Geist font"
scope: phase-1
depends_on: [fe_1.1_next_init]
estimate: 20
status: todo

content:
  - Create app/globals.css with:
      CSS custom properties (design tokens):
        --color-primary:    #F97316   (orange)
        --color-bg:         #0A0A0A   (black)
        --color-surface:    #111111   (card surface)
        --color-border:     #222222   (borders)
        --color-muted:      #6B7280   (gray)
        --color-text:       #FFFFFF   (white)
        --color-text-muted: #9CA3AF   (light gray)
        --color-error:      #EF4444   (red for errors)
        --color-success:    #22C55E   (green for success)
        --radius:           4px       (sharp, Vercel-style)
        --font-mono:        "Geist Mono", monospace
        --font-sans:        "Geist", sans-serif

      Base reset:
        - box-sizing: border-box
        - margin: 0, padding: 0
        - body: background var(--color-bg), color var(--color-text), font-family var(--font-sans)

      Utility classes:
        - .container: max-width 480px, margin auto, padding 24px
        - .card: background var(--color-surface), border 1px solid var(--color-border), border-radius var(--radius), padding 32px
        - .btn-primary: background var(--color-primary), color white, border none, padding 12px 24px, border-radius var(--radius), cursor pointer, font-weight 600
        - .btn-primary:hover: opacity 0.9
        - .input: width 100%, background #1A1A1A, border 1px solid var(--color-border), color var(--color-text), padding 10px 14px, border-radius var(--radius), font-family inherit
        - .input:focus: border-color var(--color-primary), outline none
        - .label: display block, color var(--color-text-muted), font-size 0.875rem, margin-bottom 6px
        - .error-text: color var(--color-error), font-size 0.875rem
        - .success-text: color var(--color-success), font-size 0.875rem

  - Configure Geist font in app/layout.tsx using @vercel/geist

acceptance_criteria:
  - globals.css has all CSS tokens defined
  - All utility classes defined
  - Geist font loads correctly
  - Body background is #0A0A0A (black)
@end
```
