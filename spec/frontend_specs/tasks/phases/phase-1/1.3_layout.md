```slc
@block TASK fe_1.3_layout
priority: critical
intent: "Create the root layout with Geist font, metadata, and CSP headers"
scope: phase-1
depends_on: [fe_1.2_design_system]
estimate: 10
status: todo

content:
  - Implement app/layout.tsx:
      - Import Geist and GeistMono from @vercel/geist
      - Apply font CSS variables to <html> element
      - Import globals.css
      - Set metadata: title "SLC Early Access", description "Register for early access to the SLC language & framework"
      - Render {children} inside <body>

  - Configure next.config.js:
      - Add Content Security Policy headers:
          Content-Security-Policy: "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:;"
      - Add X-Content-Type-Options: nosniff
      - Add X-Frame-Options: DENY
      - Add Referrer-Policy: strict-origin-when-cross-origin

acceptance_criteria:
  - app/layout.tsx renders without errors
  - Geist font applied to all pages
  - Security headers present on all responses (verify via browser DevTools)
  - Page title shows "SLC Early Access"
@end
```
