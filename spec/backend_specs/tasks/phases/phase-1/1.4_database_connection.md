```slc
@block TASK 1.4_database_connection
priority: critical
intent: "Set up async Neon PostgreSQL connection via SQLModel"
scope: phase-1
depends_on: [1.3_env_config]
estimate: 15
status: todo

content:
  - Implement app/core/database.py:
      - Create async SQLModel engine using settings.DATABASE_URL
      - Create async_session_maker using AsyncSession
      - Implement get_session() async generator for FastAPI Depends()
      - Implement create_db_and_tables() async function (calls SQLModel.metadata.create_all)

  - In main.py:
      - Add lifespan context manager
      - Call create_db_and_tables() on startup

  - Verify Neon connection:
      - Start uvicorn
      - Confirm startup log shows no connection errors

  notes: "Use asyncpg driver. Connection string format: postgresql+asyncpg://..."

acceptance_criteria:
  - app/core/database.py has engine, async_session_maker, get_session, create_db_and_tables
  - uvicorn starts without DB connection errors
  - get_session() yields AsyncSession correctly
@end
```
